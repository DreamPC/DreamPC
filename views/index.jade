extends layout

block content
  h3 Welcome to DreamPC!
  :marked
    Find tutorials and documentation at http://roots.cx : )

    #### A test of jQuery and Bootstrap

    Just to make sure that everything is working fine before we proceed.

  // Modal
  .modal.fade#myModal(tabindex="-1" role="dialog")
    .modal-dialog
      .modal-content
        .modal-header
          button(type="button" class="close" data-dismiss="modal" arial-label="Close")
            span(aria-hidden="true") &times;
          h4.modal-title Modal title
        .modal-body
          p One fine body &hellip;
        .modal-footer
          button(type="button" class="btn btn-default" data-dismiss="modal") Close
          button(type="button" class="btn btn-primary") Save changes
  // Modal trigger
  button(type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#myModal").
    Launch  demo modal
